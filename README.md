## TODOS
- sort the commands when retriving from config and storing them in an IndexMap instead of an HashMap to keep the Ordering 
    - [./mex-render/src/elements/which_key.rs]
    - [./mex-keys/src/keymap.rs]
